name: sampleproject

on: [push]
jobs:
  samplejob:
    runs-on: windows-2019
    steps: 
      - name: create a directory
        uses: actions/checkout@v2
        run: |
          mkdir GITHUB_WORKSPACE/sampledirectory
          echo ${env:PATH}
          touch config.yaml
        working-directory: ./temp